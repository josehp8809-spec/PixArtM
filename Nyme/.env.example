# ─── Base de datos (PostgreSQL) ───────────────────────────────────────────────
DB_HOST=localhost
DB_NAME=nyme_db
DB_USER=postgres
DB_PASS=tu_password_aqui
DB_PORT=5432

# ─── Admin inicial (se crea solo si no hay usuarios en la DB) ─────────────────
ADMIN_USER=admin
ADMIN_PASSWORD=cambia_esto_en_produccion

# ─── Meta WhatsApp API ────────────────────────────────────────────────────────
# Obtenlos en: https://developers.facebook.com/apps/
META_VERIFY_TOKEN=nyme_verify_2024
META_APP_SECRET=        # App Secret del portal de Meta (para validar firma SHA256)

# ─── Gemini AI (opcional — también se puede configurar desde el panel) ────────
GEMINI_API_KEY=

# ─── Notificaciones por Correo (SMTP) ──────────────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu_correo@gmail.com
SMTP_PASS=tu_app_password
NOTIFICATION_EMAIL=admin_pedidos@tuempresa.com

